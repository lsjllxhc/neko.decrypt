package com.neko.GUI;

//引入awt中的所有库
import java.awt.*;

public class GUI {
    public static void main(String[] args) {
        // 创建一个 Frame 对象，并设置标题
        Frame frame = new Frame("UnLocker-GUI");

        // 设置 Frame 的大小
        frame.setSize(400, 300);

        // 设置 Frame 可见
        frame.setVisible(true);
    }
}